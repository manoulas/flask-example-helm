apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-example-helm.fullname" . }}
  labels:
    {{- include "flask-example-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if .Values.service.nodePort }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "flask-example-helm.selectorLabels" . | nindent 4 }}
